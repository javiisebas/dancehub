import { BaseGetByFieldHandler, GetByFieldQuery } from '@api/common/abstract/application';
import { Inject, Injectable } from '@nestjs/common';
import { {{pascalCase name}} } from '../../domain/entities/{{kebabCase name}}.entity';
import {
    I{{pascalCase name}}Repository,
    {{constantCase name}}_REPOSITORY,
    {{pascalCase name}}Field,
    {{pascalCase name}}Relations,
} from '../../domain/repositories/i-{{kebabCase name}}.repository';

export class Get{{pascalCase name}}ByFieldQuery extends GetByFieldQuery<{{pascalCase name}}Field, {{pascalCase name}}Relations> {}

@Injectable()
export class Get{{pascalCase name}}ByFieldHandler extends BaseGetByFieldHandler<{{pascalCase name}}, {{pascalCase name}}Field, {{pascalCase name}}Relations> {
    constructor(@Inject({{constantCase name}}_REPOSITORY) repository: I{{pascalCase name}}Repository) {
        super(repository);
    }
}
