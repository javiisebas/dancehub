import { BaseFindManyHandler, FindManyQuery } from '@api/common/abstract/application';
import { Inject, Injectable } from '@nestjs/common';
import { {{pascalCase name}} } from '../../domain/entities/{{kebabCase name}}.entity';
import {
    I{{pascalCase name}}Repository,
    {{constantCase name}}_REPOSITORY,
    {{pascalCase name}}Field,
    {{pascalCase name}}Relations,
} from '../../domain/repositories/i-{{kebabCase name}}.repository';

export class FindMany{{pascalCase (pluralize name)}}Query extends FindManyQuery<{{pascalCase name}}Field, {{pascalCase name}}Relations> {}

@Injectable()
export class FindMany{{pascalCase (pluralize name)}}Handler extends BaseFindManyHandler<{{pascalCase name}}, {{pascalCase name}}Field, {{pascalCase name}}Relations> {
    constructor(@Inject({{constantCase name}}_REPOSITORY) repository: I{{pascalCase name}}Repository) {
        super(repository);
    }
}
