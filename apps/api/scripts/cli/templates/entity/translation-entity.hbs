import { BaseTranslationEntity } from '@api/common/abstract/domain';

export class {{pascalCase name}}Translation extends BaseTranslationEntity {
    constructor(
        id: string,
        locale: string,
        public name: string,
        createdAt: Date,
        updatedAt: Date,
    ) {
        super(id, locale, createdAt, updatedAt);
    }

    static create(
        id: string,
        locale: string,
        name: string,
    ): {{pascalCase name}}Translation {
        const now = new Date();
        return new {{pascalCase name}}Translation(
            id,
            locale,
            name.trim(),
            now,
            now,
        );
    }
}