import { BaseNamedTranslationDto } from '../../common'; export class
{{pascalCase name}}TranslationDto extends BaseNamedTranslationDto { // Inherits: locale, name,
description // Add specific fields for
{{name}}
here if needed in the future }