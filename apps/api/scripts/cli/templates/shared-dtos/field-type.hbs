{{#if fields}}
export type {{pascalCase name}}Field =
{{#each fields}}
    | '{{camelCase this.name}}'
{{/each}}
    | 'id'
    | 'createdAt'
    | 'updatedAt';
{{else}}
export type {{pascalCase name}}Field = 'id' | 'createdAt' | 'updatedAt';
{{/if}}